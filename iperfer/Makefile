# Compiler and flags
JAVAC = javac
JFLAGS = -g

# Source and output files
SRC = Iperfer.java
OUT = Iperfer.class

# Default target
all: $(OUT)

# Compile the source file
$(OUT): $(SRC)
	$(JAVAC) $(JFLAGS) $(SRC)

# Clean the compiled files
clean:
	rm -f $(OUT)
